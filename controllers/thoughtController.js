const { User, Thought } = require('../models');
// ?does object id go here? because of the assignment?
const { ObjectId } = require('mongoose').Types;

module.exports = {
    // Where routes will go
}